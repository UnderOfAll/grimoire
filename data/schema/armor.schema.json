{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Armor",
  "type": "object",
  "required": ["name", "category"],
  "properties": {
    "id": { "type": "string", "description": "kebab-case unique id; defaults to slug of name" },
    "name": { "type": "string" },
    "source": { "type": "string", "default": "Circus System" },
    "flavor": { "type": "string", "description": "one-line evocative summary of the armor's look/feel/circus theme" },
    "category": {
      "type": "string",
      "enum": ["clothing", "light", "medium", "heavy", "shield"],
      "description": "5e armor category. clothing = baseAC 10 + full Dex, needs NO proficiency (for casters/mages who lack armor proficiency; the unarmored baseline as an item); light = baseAC + full Dex; medium = baseAC + Dex (capped by maxDexBonus, usually 2); heavy = baseAC only, no Dex; shield = a flat acBonus added on top of worn armor."
    },
    "availability": {
      "type": "string",
      "enum": ["starter", "bought"],
      "default": "starter",
      "description": "How a character gets it. starter = basic, cheap, available at character creation (padded/leather/studded, hide/scale, chain mail, shield). bought = a premium upgrade acquired later by purchase or loot, NOT owned at creation (breastplate, half-plate, splint, plate). Roughly tracks 5e cost (starter < ~75gp; bought >= ~200gp). Actual price is left to the DM/campaign."
    },
    "baseAC": {
      "type": "integer",
      "description": "Base Armor Class before Dexterity, for body armor (light/medium/heavy). Omit for shields (use acBonus instead)."
    },
    "maxDexBonus": {
      "type": ["integer", "null"],
      "description": "Dexterity cap added to baseAC. null = no cap (light armor adds full Dex mod); 2 = medium armor; 0 = heavy armor (no Dex). Omit for shields."
    },
    "acBonus": {
      "type": "integer",
      "description": "Shields only: the flat AC bonus a shield adds (e.g. 2). Omit for body armor."
    },
    "strengthRequirement": {
      "type": "integer",
      "description": "Minimum Strength score to wear without a speed penalty; omit if none. Heavy armors are typically 13-15."
    },
    "stealthDisadvantage": {
      "type": "boolean",
      "default": false,
      "description": "true if wearing this armor imposes disadvantage on Dexterity (Stealth) checks."
    }
  }
}
